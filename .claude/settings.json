{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit*)",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$PROJECT_DIR\" && npm run build 2>&1 | tail -20; exit ${PIPESTATUS[0]}"
          }
        ]
      }
    ]
  }
}
