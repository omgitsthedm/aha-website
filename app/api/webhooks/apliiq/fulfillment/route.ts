// Canonical APLIIQ store callback path. Keep the shorter route as a harmless
// compatibility alias while the provider account is configured.
export const runtime = "nodejs";
export { GET, POST } from "../route";
